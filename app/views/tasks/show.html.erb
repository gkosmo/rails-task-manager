<h1>Tasks#show</h1>
<p>Find me in app/views/tasks/show.html.erb</p>
<p> <%= @task.title %></p>
  <p> <%= @task.percent %></p>
    <p> <%=   @task.due_date.hour%></p>
    <p> <%=   @task.due_date.min%></p>
    <p> <%=   @task.due_date.year%></p>

  <%= link_to 'Remove', task_path(@task), method: :delete, data: { confirm: 'Are you sure bro?'} %>
  <%= link_to 'Edit', edit_task_path(@task) %>
